extends layout

block content
  h1= title
  p Welcome to #{title}
  a.btn.btn-primary(href="/users") View Users
  a.btn.btn-secondary(href="/auth/login") Login
  a.btn.btn-secondary(href="/auth/register") Register